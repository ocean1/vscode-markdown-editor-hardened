# Scratchpad — vscode-markdown-editor-hardened

Project-local scratchpad. ¬shared with csi or any other project.

## Active

T0 foundation in progress: rename + scaffolding (C0.1, C0.2, C0.3). Next: push origin/master, then start T1 with C1.1 (CI fix from upstream PR #151).

## Focus

Land T0 cleanly (3 commits + first push). Then T1 starts with the CI fix.

---

| # | Cat | Item | Dep | Status |
|---|-----|------|-----|--------|
| T1 | T | Update SECURITY.md once each T1 fix lands — flip status column from "open" to "C1.N landed" | — | pending T1 |
| T2 | T | Add a CHANGELOG.md at C2.4 (T2) — track per-release changes | — | pending T2 |
| T3 | I | CSP `'unsafe-inline'` on style-src is a known trade-off (DC4 + your PD2 question response). Revisit at end of T1: was the trade-off necessary? Is vditor's style injection patchable? Investigate alternatives (hash-list per known styles, fork vditor to nonce-gate, switch markdown engine). | — | pending T1-complete |
| T4 | I | Marketplace publisher slug = "ocean1" (assumed from GH handle). Confirm before first Marketplace publish at end of T2 — Marketplace publisher slug may need separate registration via `vsce create-publisher`. | — | pending T2 publish step |
| T5 | T | `out/extension.js` will be stale after T0 source-only commits — rebuild + commit at end of T1 alongside vditor bump (C1.13). For now, anyone wanting to test locally needs to run `pnpm install && tsc` first. | T1 | pending T1 |
| T6 | T | Add `upstream-original` fetch occasionally to track new releases from zaaack — `git fetch upstream-original`, check for 0.1.15+. If upstream releases the CI fix or vditor bump first, we can rebase or drop the corresponding commits. | — | pending |
| T7 | P | Consider adding a `CONTRIBUTING.md` once we want external contributors. Not urgent — solo-maintained at T0. | — | when: project gets ≥1 external interest |
| O1 | O | H12 (Lute does not strip `command:` URIs) was confirmed by programmatic test against vditor@3.8.4 AND vditor@3.11.2. The test artifact lives at `/tmp/lute-test/test-lute-v2.js` — move into `tests/poc-h2-command-uri.js` during C1.3. | — | reference for C1.3 |
| O2 | O | Upstream PRs catalogued: #151 (CI fix — adopt as C1.1), #154 (auto-focus — merge as C1.16), #153 (find widget — merge as C1.17), #157 (line numbers — adapt for CSP, merge as C1.18). REJECT: #126 (external CSS — security-degrading), #102 (superseded by #142). #142 (vditor bump): read for hints, do fresh as C1.12. | — | reference for T1 merges |
| O3 | O | Upstream is solo-maintained, 103 open issues, last release Jan 2025 (0.1.13). Marketplace at 0.1.13; 0.1.14 in git but never published (CI broken since 0.1.14 tag — fixed by #151). Implication: hybrid-PR posture has uncertain merge probability. Track each upstream PR's response. | — | reference, ongoing |

|D|=0 |T|=4 |I|=2 |P|=1 |O|=3
